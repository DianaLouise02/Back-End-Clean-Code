const { Router} = require("express");

const router = Router();

// Função de criar
router.post('/' , );

// Função de editar
router.put('/:id', );

// Função de deletar
router.delete('/:id', );

// Função de busca única
router.get('/:id', );

// Função de busca geral

router.get('/', );

module.exports = router;